# Copy to .env.local and fill in. .env.local is gitignored.

# Auth0 tenant + SPA app
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=
VITE_AUTH0_AUDIENCE=https://api.fivenine.local

# Server base URL. Leave empty in dev to use the Vite proxy (see vite.config.ts).
VITE_API_URL=
